name: Test Runner PEC6

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Mensaje de prueba
        run: echo "GitHub Actions funcionando"
